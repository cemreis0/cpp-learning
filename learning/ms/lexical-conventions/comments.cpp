// src/ms/lexical-conventions/comments.cpp

#include <iostream>
using namespace std;

int main()
{
    // A comment is text that the compiler ignores but that is useful for programmers. 
    // Comments are normally used to annotate code for future reference. 
    // The compiler treats them as white space. 

    // You can use comments in testing to make certain lines of code inactive; 
    // however, #if/#endif preprocessor directives work better for this because 
    // you can surround code that contains comments but you cannot nest comments.

    // A C++ comment is written in one of the following ways:
    // /* any sequence of characters (including new lines) */
    // // any sequence of characters (no new line)

    return 0;
}