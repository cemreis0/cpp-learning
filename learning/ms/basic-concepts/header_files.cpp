// src/ms/header_files.cpp

int main()
{
    /*
    The following are not allowed, or are considered very bad practice to include in a header file:

    - built-in type definitions at namespace or global scope
    - non-inline function definitions
    - non-const variable definitions
    - aggregate definitions
    - unnamed namespaces
    - using directives
    */

    return 0;
}